<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xlaser4j.mybatis.mapper.write.StudentWriteMapper">

    <!--mapper.xml文件默认是在resources下,可以通过配置pom文件添加额外的resources目录,否则默认忽略src的xml-->
    <select id="listStudents" resultType="com.xlaser4j.mybatis.model.Student">
        SELECT * FROM STUDENT
    </select>
</mapper>